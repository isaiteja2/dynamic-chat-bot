# Force the installation of a newer, compatible sqlite3 binary first
pysqlite3-binary==0.5.2
# Pin chromadb to a version known to work with this binary
chromadb==0.4.24
# Add the rest of your dependencies
streamlit
openai
python-dotenv
notion-client
sentence-transformers
numpy==1.26.4
