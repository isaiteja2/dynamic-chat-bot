services:
  - type: web
    name: notion-rag-bot
    env: python
    pythonVersion: "3.11" # Explicitly set a stable Python version
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"